import { Box } from "native-base";
import { ScrollView } from "react-native";

const StatsScreen = () => {
  return (
    <ScrollView>
      <Box>Stats</Box>
    </ScrollView>
  );
};

export default StatsScreen;
