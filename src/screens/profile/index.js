import { Box } from "native-base";
import { ScrollView } from "react-native";

const ProfileScreen = () => {
  return (
    <ScrollView>
      <Box>Profile</Box>
    </ScrollView>
  );
};

export default ProfileScreen;
